/**
 * Translation utilities for the Pani platform
 * Note: Translation functionality has been removed as the platform now operates in English only
 */

/**
 * Placeholder function for translation - returns input unchanged
 * @param {string|string[]} input - Text or array of text to translate
 * @returns {Promise<string|string[]>} - Returns input unchanged
 */
export const translateText = async (input) => {
  console.warn('Translation functionality has been removed. Returning input unchanged.');
  return input;
};